# My first go lang restful api

## List of endpoint

```
GET /posts
GET /posts/:id
POST /posts
DELETE /posts/:id
PUT /posts/:id
```

## How to run

- go to directory

- run `CompileDaemon -command="./go-crud-alf"`
